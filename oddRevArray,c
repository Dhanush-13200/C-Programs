#include <stdio.h>
#include <stdlib.h>

/*
Input :
5
60  201 10  25  100

Output :
25  201

*/

int main()
{
    int x,arr[100];
    scanf("%d",&x);
    int i;
    for(i=0 ; i<x ; i++)
    {
        scanf("%d",&arr[i]);
    }
    for(i=x ; i>=0 ; i--)
    {
        if(arr[i]%2!=0)
        {
            printf("%d ",arr[i]);
        }
    }
    return 0;
}
