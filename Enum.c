#include <stdio.h>
#include <stdlib.h>

/**************
This is used to 
find the Element Stored.
***************/

int main()
{
    enum {A,B,C,D,E};
    int x,y;
    x = A;
    y = D;
    printf("The Value of A is %d\n", x);
    printf("The Value of B is %d\n", y);
    return 0;
}
